spring:
# 스프링부트는 별도의 DB 설정이 없으면 인메모리 모드로 돌림
#  datasource:
#    url: jdbc:h2:mem:test # H2 데이터베이스의 In-Memory 모드
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # 스프링부트는 기본적으로 create-drop으로 돌아감
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
    org.hibernate.orm.jdbc.bind: trace